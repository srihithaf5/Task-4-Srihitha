Task-4-srihitha

Backend
- server.js
- package.json

Frontend
- index.html
- style.css
- app.js

Setup
1. In `backend` run `npm install` to install dependencies.
2. Run `npm start` in `backend` to start the server.
3. Open `frontend/index.html` in a browser for the frontend.
